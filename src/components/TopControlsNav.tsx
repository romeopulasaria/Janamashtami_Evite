"use client";

import React from "react";
// Visual controls have been deliberately removed per the visual reference constraint.
// The audio continues to play via the AudioEngine/CinematicContext without these floating controls.

export const TopControlsNav: React.FC = () => {
  return null;
};
